# Local Artifacts

This directory is for local-only generated outputs such as:

- release build logs
- device/session logs
- crash dumps
- lint/test output captures

Everything under this directory is ignored by git except this file. It keeps the repo root clean while preserving debugging material when needed.
